
  
  // code to set confirmation
  function myFunction() {
    var txt;

    if (confirm("Are you sure to procced?")) {
      window.location.href='Voting_page.html'
    } else {
      window.location.href='Voting_home.html'
    }
  }

